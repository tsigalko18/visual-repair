package main.java.repair;

public class TestCaseRunner {

	public static void main(String[] args) {

		// step 1 : run the test suite on the correct version

		// Result result =
		// JUnitCore.runClasses(Claroline_TestSuite_Selenium.class);
		// for (Failure fail : result.getFailures()) {
		// System.out.println(fail.toString());
		// }
		// assertTrue(result.wasSuccessful());

		// step 2 : run the test suite on the buggy version

		// Result result1 =
		// JUnitCore.runClasses(Claroline_Direct_Breakage.class);
		// for (Failure fail : result1.getFailures()) {
		// printFailure(fail);
		// }
		// assertTrue(!result1.wasSuccessful());
		
		// step 3 : run the analysis and repair

	}

}
