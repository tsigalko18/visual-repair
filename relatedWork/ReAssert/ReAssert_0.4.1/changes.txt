Version 0.2.0 (2009-07-07)
 * New strategies
   * Dedicated intermediate strategy for assertNull(x) => assertEquals(null, x)
   * Attempt to remove nondeterministic assertions introduced by ReAssert
   * Strategy for ReAssert itself
 * Upgrade internal JUnit to 4.6
 * Changes to pretty-printer
   * Break strings at newlines
   * Fewer parentheses
 * On-demand parsing (much quicker!)
 * Better import calculation
   * Uses qualified name if fix introduces a type
   * Assumes Assert.* will be imported statically

Version 0.1.0 (2009-05-23)
 * Initial public release
